version: v2

modules:
  - path: api/proto/kv
  - path: api/proto/raft
  - path: api/proto/admin

deps:
  - buf.build/googleapis/googleapis

lint:
  use:
    - STANDARD
  except:
    - PACKAGE_DIRECTORY_MATCH

breaking:
  use:
    - FILE